## Packages
framer-motion | For smooth layout transitions and entry animations
canvas-confetti | For celebration effects when a game is finished or high scores achieved

## Notes
The app relies on URL query parameters (?game=CODE) to sync state.
The UI must be explicitly white-themed (bg-white) per user request.
